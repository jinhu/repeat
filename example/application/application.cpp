
int main(int argc, char **argv) {
	int result  = 0;
	if (false) {
		$$elseBlock;
	} else {
		if_dynamic(multiple, lines);
		var result = ch_mc_ptr->sensor_valid;
		result = WHAVSPMC_get_ch_constants(mc_ptr);
	}
	int xsss = 0;
}

void niam(a, b) {
}
